---
tipo: portal
titulo: "Linguagem C - Aula 04"
disciplina: "Linguagem C"
semestre: "2026.2"
data: 2026-08-20
professor: "Vinícius"
---

A quarta aula de Linguagem C com o Professor Vinícius foi focada no desenvolvimento e resolução de um **conjunto de exercícios práticos diversos** de tomada de decisão e lógica de programação em laboratório.

---

## 1. Otimização Lógica: Operadores `&&` e `||`
No início da aula, o Professor Vinícius realizou uma intervenção técnica para orientar a turma na simplificação dos códigos da lista de exercícios, evitando aninhamentos excessivos de `if`:

- **Operador Lógico E (`&&`):** Exige que todas as condições sejam simultaneamente verdadeiras para autorizar o bloco.
- **Operador Lógico OU (`||`):** Basta que uma das condições seja verdadeira para disparar a execução.
- **Comparativo Conceitual:** Vinícius comparou com o `and` / `or` do Python e com a tabela-verdade clássica da lógica booleana.

```c
#include <stdio.h>

int main(void) {
    int valor = 0;

    printf("=== CONJUNTO DE EXERCÍCIOS: CLASSIFICAÇÃO NUMÉRICA ===\n");
    printf("Digite um número inteiro: ");
    scanf("%d", &valor);

    // Validação elegante de intervalo em linha única
    if (valor >= 10 && valor <= 50) {
        printf("Resultado: O valor %d está DENTRO do intervalo permitido [10, 50].\n", valor);
    } else if (valor < 0 || valor > 100) {
        printf("Resultado: O valor %d é um caso de extremidade (< 0 ou > 100).\n", valor);
    } else {
        printf("Resultado: O valor %d está em faixa intermediária fora do intervalo [10, 50].\n", valor);
    }

    return 0;
}
```

---

## 2. Resolução do Conjunto de Exercícios Práticos Diversos
A turma trabalhou em uma bateria diversificada de problemas em laboratório:

- **Classificação de Faixas Etárias e Categorias:** Validações compostas com limites superiores e inferiores.
- **Cálculo de Descontos e Tarifações:** Regras condicionais para determinar alíquotas com base no valor de entrada.
- **Validação de Limites de Entrada:** Checagem de integridade dos dados fornecidos pelo usuário antes de efetuar cálculos.

---

## Glossario

- **Operador Lógico E (&&)**: Conjunção lógica em C que retorna verdadeiro apenas se ambos os operandos forem verdadeiros [Wikipedia](https://pt.wikipedia.org/wiki/Conjun%C3%A7%C3%A3o_l%C3%B3gica).
- **Operador Lógico OU (||)**: Disjunção lógica em C que retorna verdadeiro se ao menos um dos operandos for verdadeiro [Wikipedia](https://pt.wikipedia.org/wiki/Disjun%C3%A7%C3%A3o_l%C3%B3gica).
- **Tabela-Verdade**: Tabela matemática utilizada na lógica booleana para mapear os resultados de operações lógicas [Wikipedia](https://pt.wikipedia.org/wiki/Tabela-verdade).
